<?xml version="1.0" encoding="utf-8"?>
<!-- Only the Administrator app can turn the phone into the school's SMS sender. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Still installable on tablets / phones without a SIM; they just can't act as the SMS sender -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application>
        <!-- Sends the school's SMS codes through this phone's SIM while it is switched on in the app -->
        <service
            android:name="com.school.sms.SmsGatewayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="sms_gateway" />
        </service>
    </application>
</manifest>
